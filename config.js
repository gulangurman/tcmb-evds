const config = {
    db: {
        /* this is for demo purposes */
        connectionLimit: 100,
        host: "localhost",
        user: "user",
        password: "password",
        database: "dbname",
    },
    listPerPage: 10,
    port: 3000,
};
module.exports = config;